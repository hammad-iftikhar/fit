{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    // node types are for src/*.test.ts, which run under `node --test`.
    "types": ["node"]
  },
  "include": ["**/*.ts", "**/*.tsx", ".expo/types/**/*.ts", "expo-env.d.ts"],
  "exclude": ["node_modules"]
}
